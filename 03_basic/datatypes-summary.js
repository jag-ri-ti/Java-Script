// Primitive 

// 7 types : String, Number, Boolean, null, undefined, Symbol, BigInt
const score = 100
const scorevalue = 100.3

const isLoggedIn = false
const outsideTemp = null
let userEmail; //undefined //or letEmail = undefined ; 
 
const id = Symbol('123')
const anotherId = Symbol('123')

console.log(id === anotherId);

//const bigNumber = 1234567890987654321n
// Reference (Non primitive)

//References (Non primitive )

// Arrays, objects,  (web events or browser events and Objects Master===JS master), Functions

const heros = ["Thor", "Captain", "Ironman"];

let muObj={
    name: "Jango",
    age: 21
}

const myfunction = function(){
    console.log("Hello Future Me");
}
